# docs/

This directory is the GitHub Pages source for the Research Claim Auditor project website. `index.html` is a single self-contained file — all CSS and JavaScript are embedded inline — so it deploys directly to GitHub Pages without a build step. To enable GitHub Pages, go to **Settings → Pages** in the repository, set the source branch to `main` and the folder to `/docs`.
